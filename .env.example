# ============================================================
# 🔑 Credenziali OpenAI (Obbligatorie)
# ============================================================
# Inserisci qui la tua chiave API OpenAI
OPENAI_API_KEY="INSERISCI-LA-TUA-KEY-QUI"

# Modello realtime da usare (se non impostato usa il default nel codice)
# Puoi controllare i modelli disponibili su: https://platform.openai.com/docs/models
OPENAI_REALTIME_MODEL=gpt-4o-mini-realtime-preview-2024-12-17

# Voce TTS per la sintesi vocale
# Opzioni: alloy, echo, fable, onyx, nova, shimmer, coral, verse, ballad, ash, sage
OPENAI_VOICE=verse

# ============================================================
# 🎙️ Configurazione Audio e VAD
# ============================================================
# Sample rate microfono
#   - 24000 su PC/Raspberry Pi 4/5
#   - 16000 su Raspberry Pi 3
SAMPLE_RATE=24000

# Dimensione dei chunk audio catturati (in millisecondi)
CHUNK_MS=20   # valori consigliati: 20–40

# Parametri VAD (Voice Activity Detection) lato server
VAD_THRESHOLD=0.2      # soglia sensibilità
VAD_SILENCE_MS=800     # durata silenzio per fine turno
VAD_PREFIX_MS=500      # padding iniziale

# Minimo caratteri prima di stampare la trascrizione utente
MIN_USER_CHARS=6

# Buffer massimo di audio TTS (in secondi)
OUTPUT_MAX_BUFFER_SEC=20

# Ritardo tra fine TTS e riattivazione microfono (in ms)
LISTEN_COOLDOWN_MS=1000

# ============================================================
# 🌐 Timeout & Connessione
# ============================================================
RECONNECT_MAX_BACKOFF=30
SEND_TIMEOUT=5.0
RECV_TIMEOUT=150.0
PING_INTERVAL=20.0
PING_TIMEOUT=20.0
OPEN_TIMEOUT=20.0
CLOSE_TIMEOUT=5.0

# ============================================================
# 🧠 Memoria conversazione locale
# ============================================================
# 1 = abilita persistenza locale, 0 = disabilita
PERSIST_MEMORY=1

# Numero massimo di turni memorizzati (conversazione locale)
MEMORY_TURNS=8

# ============================================================
# 🤖 RobotLink (connessione seriale con Arduino)
# ============================================================
# Porta seriale del robot:
#   - "auto" = auto-detect (consigliato)
#   - es. COM3 (Windows) oppure /dev/ttyACM0 (Linux)
ROBOT_PORT=auto

# Filtri facoltativi per l’auto-detect
ROBOT_VENDOR=
ROBOT_PRODUCT=

# Serial number specifico (se vuoi collegarti a un solo device)
ROBOT_SERIAL=

# ============================================================
# 🎚️ VAD locale (opzionale, filtra silenzi PRIMA dell’invio)
# ============================================================
# Consigliato su PC e Raspberry Pi 4/5
# Disattivalo su Pi3 o macchine lente
OPENAI_LOCAL_VAD=1

# Parametri VAD locale
LOCAL_VAD_ALPHA=0.2        # Fattore EMA (rumore di fondo)
LOCAL_VAD_THR_ON=0.65      # Soglia per attivare la voce
LOCAL_VAD_THR_OFF=0.45     # Soglia per disattivare la voce
LOCAL_VAD_MIN_VOICE_MS=120 # Minimo durata voce (ms)
LOCAL_VAD_MIN_SIL_MS=250   # Minimo durata silenzio (ms)

# ============================================================
# 🖥️ HUD Diagnostico
# ============================================================
# Abilita (1) o disabilita (0) la finestra HUD
HUD=1

# Modalità HUD:
#   - cv      = finestra grafica con OpenCV
#   - console = stampa su terminale
#   - auto    = sceglie automaticamente in base al sistema
HUD_MODE=cv


